class SettingData {
  bool isActived;
  bool switchValue;
  String title;
  String subTitle;
  String image;

  SettingData(
      this.isActived, this.switchValue, this.title, this.subTitle, this.image);
}
